{
  "routes": [
    {
      "method": "GET",
      "path": "/restorents",
      "handler": "restorents.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/restorents/count",
      "handler": "restorents.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/restorents/:id",
      "handler": "restorents.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/restorents",
      "handler": "restorents.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/restorents/:id",
      "handler": "restorents.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/restorents/:id",
      "handler": "restorents.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
